# Profile Image

Place your profile image here as `profile.jpg`

The current implementation uses external URLs from randomuser.me and unsplash.com.
For production, replace these with local images in the `/public` directory.

Example usage in components:
```jsx
<img src="/profile.jpg" alt="Suleman Farooq" />
```